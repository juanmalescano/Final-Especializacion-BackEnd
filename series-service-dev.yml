server:
  port: 8086
  
spring:
  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      password: rootpass
      database: tp_final
      port: 27017
      host: localhost

info:
  app:
    name: ${spring.application.name}
    version: 1.0.0
    description: Series Microservice
